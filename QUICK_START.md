# 🚀 دليل التشغيل السريع - INVOCCA

## ✅ ما تم إنجازه

تم ربط **لوحة تحكم المدير** بالكامل مع نظام التوجيه (Routes)!

---

## 📋 التغييرات التي تمت

### 1. **تفعيل Routes المدير**
📄 `src/App.jsx`

```javascript
// تم تفعيل هذه الأسطر:
<Route element={<ProtectedRoute requiredRole={USER_ROLES.MANAGER}><MainLayout /></ProtectedRoute>}>
    <Route path="/manager" element={<Navigate to={ROUTES.MANAGER.DASHBOARD} replace />} />
    <Route path={ROUTES.MANAGER.DASHBOARD} element={<ManagerDashboard />} />
    <Route path={ROUTES.MANAGER.HALL} element={<HallManagement />} />
    <Route path={ROUTES.MANAGER.EVENTS} element={<EventsManagement />} />
    <Route path={ROUTES.MANAGER.CLIENTS} element={<ClientsManagement />} />
    <Route path={ROUTES.MANAGER.STAFF} element={<StaffManagement />} />
</Route>
```

### 2. **تحديث قائمة Sidebar**
📄 `src/components/layout/menuConfig.js`

```javascript
manager: [
    { label: 'الرئيسية', path: ROUTES.MANAGER.DASHBOARD, icon: LayoutDashboard },
    { label: 'الفعاليات', path: ROUTES.MANAGER.EVENTS, icon: ClipboardList },
    { label: 'العملاء', path: ROUTES.MANAGER.CLIENTS, icon: UserPlus },
    { label: 'الموظفون', path: ROUTES.MANAGER.STAFF, icon: Users },
    { label: 'القاعة', path: ROUTES.MANAGER.HALL, icon: Building2 },
]
```

### 3. **ربط الصفحات الجديدة**
```javascript
import ManagerDashboard from './pages/manager/ManagerDashboard'
import HallManagement from './pages/manager/HallManagementNew'
import EventsManagement from './pages/manager/EventsManagementNew'
import ClientsManagement from './pages/manager/ClientsManagementNew'
import StaffManagement from './pages/manager/StaffManagementNew'
```

---

## 🚀 كيفية التشغيل

### 1. **تثبيت التبعيات** (إذا لم تكن مثبتة)
```bash
npm install
```

### 2. **تشغيل المشروع**
```bash
npm run dev
```

### 3. **فتح المتصفح**
```
http://localhost:5173
```

---

## 🔐 تسجيل الدخول كمدير

للوصول إلى لوحة تحكم المدير، تحتاج إلى:

1. **تسجيل الدخول** بحساب مدير (Manager)
2. سيتم توجيهك تلقائياً إلى `/manager/dashboard`

### بيانات تجريبية (إذا كانت متوفرة):
```
البريد: manager@invocca.com
كلمة المرور: ********
```

---

## 📱 الصفحات المتاحة الآن

### ✅ لوحة تحكم المدير

| الصفحة | المسار | الوصف |
|--------|--------|-------|
| 🏠 الرئيسية | `/manager/dashboard` | لوحة التحكم الرئيسية مع الإحصائيات |
| 📅 الفعاليات | `/manager/events` | إدارة الفعاليات والحجوزات |
| 👥 العملاء | `/manager/clients` | إدارة العملاء |
| 👨‍💼 الموظفون | `/manager/staff` | إدارة الموظفين |
| 🏛️ القاعة | `/manager/hall` | معلومات القاعة والخدمات |

---

## 🎨 المميزات

### 1. **لوحة التحكم الرئيسية**
- ✨ بطاقات إحصائيات تفاعلية
- 📊 مؤشرات الاتجاه (Trends)
- 🏛️ معلومات القاعة
- 📅 الحجوزات الأخيرة
- 📱 متجاوب بالكامل

### 2. **صفحة الفعاليات**
- 🎯 عرض بطاقات الفعاليات
- 🔍 بحث متقدم
- 📊 إحصائيات سريعة
- 🏷️ شارات الحالة الملونة
- 👁️ أزرار عرض/تعديل

### 3. **صفحة العملاء**
- 👥 بطاقات مع صور رمزية
- 📞 معلومات الاتصال
- 📊 إحصائيات (نشط/غير نشط)
- 🔍 بحث شامل

### 4. **صفحة الموظفين**
- 👨‍💼 بطاقات الموظفين
- 🎭 شارات الأدوار
- 📊 إحصائيات مفصلة
- 🔍 بحث متقدم

### 5. **صفحة القاعة**
- 🏛️ معلومات كاملة
- 📊 بطاقات إحصائية
- ✨ قسم الخدمات
- ✏️ وضع التعديل

---

## 🔧 استكشاف الأخطاء

### المشكلة: لا أرى لوحة تحكم المدير

**الحل:**
1. تأكد من تسجيل الدخول بحساب مدير (`role: 'manager'`)
2. تحقق من Console في المتصفح للأخطاء
3. تأكد من أن API يعمل بشكل صحيح

### المشكلة: الصفحة فارغة أو بيضاء

**الحل:**
1. افتح Console في المتصفح (F12)
2. ابحث عن أخطاء JavaScript
3. تأكد من أن جميع التبعيات مثبتة

### المشكلة: لا توجد بيانات

**الحل:**
1. تحقق من اتصال API
2. تأكد من أن Backend يعمل
3. راجع Network Tab في Developer Tools

---

## 📝 ملاحظات مهمة

### 1. **API Integration**
جميع الصفحات مربوطة مع API:
- `getManagerEvents()` - جلب الفعاليات
- `getClients()` - جلب العملاء
- `getStaff()` - جلب الموظفين
- `getHallInfo()` - جلب معلومات القاعة

### 2. **React Query**
يتم استخدام React Query لإدارة البيانات:
```javascript
const { data, isLoading } = useQuery({
    queryKey: [QUERY_KEYS.MANAGER_EVENTS],
    queryFn: getManagerEvents,
})
```

### 3. **Protected Routes**
جميع صفحات المدير محمية:
```javascript
<ProtectedRoute requiredRole={USER_ROLES.MANAGER}>
```

---

## 🎯 الخطوات التالية (اختياري)

### يمكنك الآن:

1. **اختبار الصفحات** ✅
   - تسجيل دخول كمدير
   - التنقل بين الصفحات
   - اختبار البحث والفلترة

2. **إضافة Form Dialogs** 
   - نماذج إضافة فعالية جديدة
   - نماذج تعديل العملاء
   - نماذج إضافة موظفين

3. **إضافة Confirmation Dialogs**
   - تأكيد الحذف
   - تأكيد التعديلات

4. **بناء لوحات أخرى**
   - لوحة تحكم Admin (محسّنة)
   - لوحة تحكم Client
   - لوحة تحكم Employee

---

## 📞 الدعم

إذا واجهت أي مشاكل:

1. **تحقق من Console** في المتصفح
2. **راجع Network Tab** للتأكد من API
3. **راجع الكود** في الملفات المذكورة أعلاه

---

## ✨ النتيجة

✅ **Routes مفعّلة**
✅ **Sidebar محدّثة**
✅ **جميع الصفحات مربوطة**
✅ **جاهز للاستخدام!**

---

**🎉 الآن يمكنك تشغيل المشروع ورؤية لوحة تحكم المدير بالكامل!**

```bash
npm run dev
```

ثم سجل دخول كمدير وستجد جميع الصفحات الجديدة! 🚀

