# أماكن اختيار القالب لكرت الدعوة

## نظرة عامة
يتم اختيار القالب من داخل واجهة عرض كرت الدعوة (`InvitationCardView`). واجهة اختيار القالب موجودة في **Header** أعلى الكرت.

---

## 1. طرق الوصول إلى واجهة اختيار القالب

### أ. بعد إنشاء دعوة جديدة:
```javascript
// في handleCreateSubmit
const result = await handleCreate(submitData)
if (result?.success !== false) {
  closeDialog()
  // فتح كرت الدعوة تلقائياً بعد الإنشاء
  if (result?.invitation) {
    setSelectedInvitation(result.invitation)
    setShowInvitationCard(true) // ← يفتح واجهة اختيار القالب
  }
}
```

**الموقع**: بعد النقر على "إضافة" في ديالوج إنشاء دعوة جديدة

---

### ب. من بطاقة الدعوة (Card View):
```javascript
// في بطاقة الدعوة
<MuiIconButton
  size="small"
  onClick={() => {
    setSelectedInvitation(invitation)
    setShowInvitationCard(true) // ← يفتح واجهة اختيار القالب
  }}
  title="عرض الكرت"
>
  <Eye size={16} />
</MuiIconButton>
```

**الموقع**: 
- في صفحة "دعواتي" (`/client/invitations`)
- في كل بطاقة دعوة
- أيقونة "عرض" (Eye icon) في قسم الإجراءات

---

## 2. موقع واجهة اختيار القالب

### داخل `InvitationCardView`:
```javascript
function InvitationCardView({ open, onClose, invitation, ... }) {
  return (
    <MuiDialog open={open} onClose={onClose}>
      {/* Header with Template Selection */}
      <MuiBox sx={{ position: 'sticky', top: 0, zIndex: 10, p: 3 }}>
        {/* ← هنا واجهة اختيار القالب */}
        {availableTemplates.length > 0 && (
          <MuiBox>
            {/* عرض القوالب كبطاقات مع معاينة */}
          </MuiBox>
        )}
      </MuiBox>
      
      {/* كرت الدعوة */}
      <MuiBox>
        {/* محتوى الكرت */}
      </MuiBox>
    </MuiDialog>
  )
}
```

**الموقع**: 
- في **Header** أعلى كرت الدعوة
- يظهر في أعلى الصفحة (sticky position)
- يحتوي على:
  - زر "العودة"
  - واجهة اختيار القالب (بطاقات القوالب)
  - أزرار التصدير (PNG, PDF)

---

## 3. تدفق العمل الكامل

```
1. المستخدم في صفحة "دعواتي"
   ↓
2. يختار أحد الخيارات:
   أ) ينقر "إنشاء دعوة جديدة" → يملأ البيانات → ينقر "إضافة"
   ب) ينقر على أيقونة "عرض الكرت" في بطاقة دعوة موجودة
   ↓
3. يتم فتح InvitationCardView (ديالوج كامل الشاشة)
   ↓
4. في Header يظهر:
   - زر "العودة" (على اليسار)
   - واجهة اختيار القالب (في الوسط)
     • عنوان "اختر القالب"
     • بطاقات القوالب مع معاينة الصور
   - أزرار التصدير PNG/PDF (على اليمين)
   ↓
5. المستخدم ينقر على بطاقة قالب
   ↓
6. يتم تحديث selectedTemplateId
   ↓
7. يتم تحديث خلفية كرت الدعوة بالقالب المحدد
   ↓
8. يمكن للمستخدم:
   - اختيار قالب آخر
   - تصدير الكرت كـ PNG
   - تصدير الكرت كـ PDF
   - العودة للصفحة الرئيسية
```

---

## 4. وصف واجهة اختيار القالب

### المكونات:
1. **عنوان**: "اختر القالب"
2. **بطاقات القوالب**: 
   - عرض بصري لكل قالب
   - معاينة الصورة (120x160 بكسل)
   - اسم القالب في overlay
   - مؤشر الاختيار (✓) للقالب المحدد
3. **تأثيرات Hover**: 
   - تكبير ورفع البطاقة
   - تغيير لون الحد إلى الذهبي

### التصميم:
- **الموقع**: Header sticky في أعلى ديالوج كرت الدعوة
- **التخطيط**: Grid responsive (flexWrap)
- **الألوان**: متسقة مع التصميم العام (ذهبي/أسود)

---

## 5. ملخص الأماكن

| الموقع | الطريقة | الكود |
|--------|---------|-------|
| **بعد إنشاء دعوة** | تلقائي | `setShowInvitationCard(true)` في `handleCreateSubmit` |
| **من بطاقة الدعوة** | أيقونة "عرض" | `onClick` في `MuiIconButton` مع `Eye` icon |
| **داخل واجهة الكرت** | Header | `InvitationCardView` → Header → Template Selection |

---

## 6. الشاشات

### أ. صفحة الدعوات الرئيسية:
```
┌─────────────────────────────────────┐
│  دعواتي                             │
│  [إنشاء دعوة جديدة]                 │
├─────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐         │
│  │ دعوة 1  │  │ دعوة 2  │         │
│  │ [عرض 👁]│  │ [عرض 👁]│  ← هنا │
│  └─────────┘  └─────────┘         │
└─────────────────────────────────────┘
```

### ب. واجهة كرت الدعوة:
```
┌─────────────────────────────────────┐
│ [العودة]  [اختر القالب]  [PNG][PDF]│ ← Header
│  ┌──┐ ┌──┐ ┌──┐                    │
│  │📄│ │📄│ │📄│  ← بطاقات القوالب  │
│  └──┘ └──┘ └──┘                    │
├─────────────────────────────────────┤
│                                     │
│      ┌─────────────────┐           │
│      │                 │           │
│      │   كرت الدعوة    │           │
│      │   (مع القالب)   │           │
│      │                 │           │
│      └─────────────────┘           │
│                                     │
└─────────────────────────────────────┘
```

---

## الخلاصة

**يتم اختيار القالب من:**
1. ✅ **داخل واجهة عرض كرت الدعوة** (`InvitationCardView`)
2. ✅ **في Header أعلى الكرت** (sticky position)
3. ✅ **عبر بطاقات القوالب** مع معاينة الصور

**الوصول إلى واجهة اختيار القالب:**
- بعد إنشاء دعوة جديدة (تلقائياً)
- من أيقونة "عرض الكرت" في بطاقة الدعوة

